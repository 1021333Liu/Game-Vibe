# 给 AI / 同伴的协作提示模板（中文）

## 目标

- （写清楚要做什么功能或修什么 bug，例如：增加计分板、换关卡布局、调碰撞手感。）

## 约束

- 技术栈：Kaboom（v3000）+ Vite + 纯 JavaScript，不改 `package.json` 里已锁版本，除非老师允许。
- 文件范围：主要改 `src/main.js`（或列出允许动的文件）。
- 性能 / 复杂度：课堂时间内可完成，避免引入新大型依赖。

## 验收标准

- `npm run dev` 无报错；交互与「目标」段落一一对应可演示。
- 关键参数（如速度常量）有注释或 README 说明，方便复查。

## 回滚方式

- 若改坏：执行 `git checkout -- src/main.js`（或 `git reset --hard` 到打好的 `class-baseline` 标签），再重新应用最小改动。
