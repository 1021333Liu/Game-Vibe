# 课堂验收清单（复制使用）

请在下课前自检并勾选：

- [ ] 执行 `npm run dev` 后浏览器能打开游戏画面，无控制台报错。
- [ ] 能用 **WASD** 或 **方向键** 控制方块移动，手感与预期一致。
- [ ] 碰到 **绿色目标** 后出现胜利提示或胜利场景，逻辑可重复触发（如按 R 重来）。
- [ ] 若实现了失败条件（如红块），行为符合设计说明，且不会与胜利逻辑冲突。
- [ ] 已把本次改动 **保存并提交**（或按老师要求打包），并能在另一台机器用 `npm ci` / `npm install` 复现。
